# 07 — 多渠道配置实战

## 概述

OpenClaw 支持同时配置多个渠道（Channel），允许一个 Agent 实例通过多种方式与用户交互。本文保留微信 + QQ Bot 的历史部署经验，但 YAML 字段、端口、回调方式和内部 API 不能视为当前版本的可复制配置；实际接入必须先查当前渠道插件文档与 schema。

多渠道配置的核心不是“全部打开”，而是：每个渠道有明确账号边界、模型路由、去重策略、外发范围、故障日志和回滚方式。

## 双渠道架构

```
┌──────────────────────────────────────────┐
│            OpenClaw Gateway               │
│                                            │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │ WeChat   │  │ QQ Bot   │  │ Web    │  │
│  │ Channel  │  │ Channel  │  │ UI     │  │
│  └────┬─────┘  └────┬─────┘  └───┬────┘  │
│       │             │            │        │
│  ┌────▼─────────────▼────────────▼─────┐  │
│  │         Agent (default)             │  │
│  │    model: deepseek/deepseek-v4-pro  │  │
│  └──────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

## 微信渠道配置

### 前置条件

1. 微信企业号/服务号
2. 微信公众平台配置回调 URL
3. Token 和 EncodingAESKey

### 历史架构示意（非当前版本复制模板）

```yaml
channels:
  - id: wechat
    name: "微信助手"
    type: weixin
    config:
      appId: "wx**"
      appSecret: "***"
      token: "***"
      encodingAESKey: "***"
      callbackUrl: "https://your-domain.com/wechat/callback"

    # 消息处理
    deduplicateWindow: 5000       # 5 秒去重窗口
    maxMessageLength: 600         # 单条回复最大长度（字符）
    splitLongMessage: true        # 自动拆分长消息

    # 图片处理
    imageServer: "https://your-domain.com/images"
    maxImageSize: 2097152         # 2MB

    # 关联的 agent
    defaultAgent: default
```

### 微信消息格式限制

| 限制项 | 数值 |
|--------|------|
| 文本消息最大长度 | 2048 字符 |
| 图片最大尺寸 | 2MB |
| 每天主动推送次数 | 取决于账号类型 |
| 回复超时 | 5 秒（被动回复） |

**重要**：微信的被动回复模式下，必须在 5 秒内返回。超时后微信会重发消息（导致重复回复问题）。建议：

```yaml
channels:
  - id: wechat
    config:
      replyMode: "async"          # 异步回复模式
      acknowledgeTimeout: 3000    # 3 秒内确认收悉
```

## QQ Bot 渠道配置

### 前置条件

1. QQ Bot AppId 和 Token
2. QQ 开放平台注册应用

### 历史架构示意（非当前版本复制模板）

```yaml
channels:
  - id: qqbot
    name: "QQ 助手"
    type: qqbot
    config:
      appId: 1903992323          # QQ Bot AppId
      appSecret: "***"
      token: "***"

    # 消息处理
    deduplicateWindow: 5000
    maxMessageLength: 2000       # QQ 消息限制更宽松

    # 图片处理
    imageServer: "https://your-domain.com/images"

    defaultAgent: default
```

### QQ Bot 消息渲染特点

与微信不同，QQ Bot 的 Markdown 渲染能力有限：

| 格式 | 微信 | QQ Bot |
|------|------|--------|
| 纯 URL（独立行） | ✅ 可点击 | ✅ 可点击 |
| 内联 URL | ✅ 可点击 | ❌ 纯文本 |
| 图片 | ✅ 显示 | ✅ 显示 |
| Markdown 表格 | ✅ | ⚠️ 部分支持 |
| 代码块 | ✅ | ⚠️ 格式不完整 |
| 嵌套列表 | ✅ | ❌ 不支持 |

#### URL 渲染问题

QQ Bot 只将**独立行的纯 URL**渲染为可点击链接。内联 URL（如 `查看详情：https://...`）保持纯文本。

**适配方案**：在发送前处理 URL，确保在独立行：

```python
def qq_url_formatter(text: str) -> str:
    """Ensure URLs are on separate lines for QQ Bot."""
    import re
    urls = re.findall(r'https?://[^\s)>]+', text)
    for url in urls:
        if not re.search(rf'(^|\n){re.escape(url)}($|\n)', text):
            text = text.replace(url, f'\n{url}\n')
    return re.sub(r'\n{3,}', '\n\n', text).strip()
```

## 双渠道共用 Agent

### 配置

```yaml
# 渠道配置
channels:
  - id: wechat
    name: "微信助手"
    type: weixin
    config:
      appId: "wx**"
      # ...
    defaultAgent: default

  - id: qqbot
    name: "QQ 助手"
    type: qqbot
    config:
      appId: 1903992323
      # ...
    defaultAgent: default

# Agent 配置（两个渠道共用）
agents:
  default:
    model: deepseek/deepseek-v4-pro
    systemPrompt: |
      你是一个智能助手，支持微信和 QQ 两个渠道。
      注意 QQ Bot 的消息渲染限制：URL 放在独立行。
```

### 渠道感知

为了让 Agent 知道当前消息来自哪个渠道，在 System Prompt 中注入渠道信息：

```yaml
agents:
  default:
    systemPrompt: |
      你是一个智能助手。当前通过 {{channelName}} 渠道与用户交互。
      {{#if channelName == "QQ 助手"}}
      QQ Bot 消息规则：
      - URL 必须放在独立行才能渲染为链接
      - 不支持嵌套列表和复杂表格
      {{/if}}
```

## 图片服务配置

### 独立图片服务器

```yaml
# openclaw.yaml
imageServer:
  baseUrl: "https://your-domain.com/images"
  storagePath: "/var/www/images"
  maxFileSize: 2097152       # 2MB
  allowedTypes:
    - image/png
    - image/jpeg
    - image/gif
    - image/webp
```

### Nginx 配置

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    location /images/ {
        alias /var/www/images/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

## TTS 代理配置

### 场景

需要使用 edge-tts（微软文字转语音）但需要代理。

### 架构

```
OpenClaw → Edge TTS Proxy (Python, port 5050) → Microsoft TTS API
                 ↓
          Mihomo Proxy (127.0.0.1:7890)
```

### 启动 TTS 代理

```bash
# 安装
pip install edge-tts fastapi uvicorn

# 代理脚本
cat > /usr/local/bin/tts_proxy.py <<'PYEOF'
import asyncio
import edge_tts
from fastapi import FastAPI, HTTPException
from fastapi.responses import StreamingResponse
import io
import os

app = FastAPI()

# 代理配置
PROXY = os.environ.get("HTTP_PROXY", "http://127.0.0.1:7890")

@app.post("/v1/audio/speech")
async def text_to_speech(request: dict):
    text = request.get("input", "")
    voice = request.get("voice", "zh-CN-XiaoxiaoNeural")
    rate = request.get("speed", "+0%")

    if not text:
        raise HTTPException(status_code=400, detail="Missing 'input'")

    try:
        communicate = edge_tts.Communicate(text, voice, rate=rate,
                                           proxy=PROXY)
        audio_data = io.BytesIO()
        async for chunk in communicate.stream():
            if chunk["type"] == "audio":
                audio_data.write(chunk["data"])

        audio_data.seek(0)
        return StreamingResponse(
            audio_data,
            media_type="audio/mpeg",
            headers={"Content-Disposition": "attachment; filename=speech.mp3"}
        )
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=5050)
PYEOF

# systemd 服务
cat > /etc/systemd/system/tts-proxy.service <<'EOF'
[Unit]
Description=Edge TTS Proxy
After=network.target

[Service]
Type=simple
User=sandbox
Environment="HTTP_PROXY=http://127.0.0.1:7890"
Environment="HTTPS_PROXY=http://127.0.0.1:7890"
ExecStart=/usr/bin/python3 /usr/local/bin/tts_proxy.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now tts-proxy
```

### OpenClaw TTS 配置

```yaml
tts:
  provider: custom
  endpoint: "http://127.0.0.1:5050/v1/audio/speech"
  voice: "zh-CN-XiaoxiaoNeural"
  speed: "+10%"
```

## 最小变更、验证与回滚

### 最小变更

1. 先接入一个渠道和一个测试账号；
2. 只配置基本收发，不同时增加图片服务、TTS、隧道或渠道级模型切换；
3. 每个公网回调、反向隧道、图片服务都单独记录暴露范围、鉴权方式与关闭方法；
4. 把渠道差异（消息长度、渲染、时限、主动推送规则）写进对应渠道的验收项。

### 验证

- 用测试账号完成一次短文本入站和一次可控出站；
- 检查是否重复回复、是否命中正确 Agent/模型、是否存在泄露到其他渠道的上下文；
- 图片、TTS、长消息拆分与主动推送分别独立验证；
- 验证日志和失败告警的位置，而不只看“能回复”。

### 回滚

- 先停用刚新增的渠道绑定或外部服务，再恢复上一份最小配置；
- 保留故障时间段日志用于去重、超时和权限排查；
- 不通过 `kill`、多重重启或同时改渠道/模型/代理来处理故障；
- 回调 URL、隧道和图片服务不再使用时应显式关闭。

---

## 多渠道管理命令

### 查看渠道状态

```bash
openclaw channel list
# 输出:
# wechat    weixin    ● connected    default
# qqbot     qqbot     ● connected    default
```

### 查看渠道日志

```bash
openclaw channel logs wechat --tail 20
openclaw channel logs qqbot --tail 20
```

### 测试渠道连通性

```bash
# 通过内部 API 测试
curl -X POST http://127.0.0.1:18789/api/test-channel \
  -H "Content-Type: application/json" \
  -d '{"channelId": "wechat"}'
```

## 运维检查清单

- [ ] Mihomo 代理正常运行
- [ ] 微信 Bridge 连接状态正常
- [ ] QQ Bot WebSocket 连接正常
- [ ] 图片服务 Nginx 可访问
- [ ] TTS 代理服务运行
- [ ] 消息去重已配置
- [ ] Agent System Prompt 包含渠道感知逻辑
- [ ] URL 格式化脚本已集成（QQ Bot）

## 参考

> 命令、端口和内部测试 API 同样具有版本差异。运行前使用当前版本的帮助、状态检查或官方文档确认；不要把本章的历史命令当作唯一入口。

- [渠道配置基础](03-渠道配置.md)
- [代理网络配置](05-代理网络实战.md)
- [常见问题排错](06-常见问题排错.md)
- QQ Bot AppId: 1903992323
